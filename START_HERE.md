# START_HERE.md — autonomous pipeline implementation tracker

**Purpose:** any agent opening this repo resumes from the "Next" line at the bottom. Do not redo completed work; verify it and move on.

**Canonical spec:** `docs/superpowers/specs/2026-09-19-autonomous-discovery-question-quality-design.md`
**Runbook:** `docs/quizzeira-pipeline-runbook.md`
**Draft PR:** https://github.com/Guilheeeeeeerme/quizzeira/pull/28 (branch `feat/autonomous-discovery-question-quality`)
**Rule:** one commit per checkpoint (§12 rollout slices). User-facing text in English. No question-bank seeds, no cross-plane DB wiring, no unattended autonomy defaults.

## Done (committed, verify before touching)

| Slice | Commit (PR branch) | What | Verify |
| --- | --- | --- | --- |
| 1 | `6d8df99` | worker-kit safety foundation: single-flight `runLoop` (overlap logged, never stacked), provider circuit breaker (`circuit.ts`, auth/billing → open 6 h, transient → backoff, 1 half-open probe), atomic non-incrementing budget reserve (Lua), auth/billing non-deferrable, log severity/version/env, pipeline-output tx timeout 120 s | `npm test -w @quizzeira/worker-kit` (70/71; 1 pre-existing fixture env fail) |
| 2 | `d23566f` | `CONTENT_WORKER_PROFILE=all\|documents\|embeddings\|generation` one-image entrypoints; compose `shadow` profile services (`content-worker-documents/-embeddings/-generation`) | `docker compose config -q` |
| 3 | `5ab42bb` | `CanonicalTopic`/`SyllabusTopicMap`/`QuestionApplicability` + additive migration `20260919000000_canonical_bank`; idempotent `/internal/canonical/backfill`; `/published/sample` dual-read via active applicability | `npm run typecheck` |
| 4 | `5e8769f` | `SourceCandidate` + migration; deterministic `scoreCandidate`/`hardDeny` (scouting.spec 5/5); `/internal/scout/candidates` observe-only (`DISCOVERY_SCOUT_AUTO_ACTIVATE` gates activation) | `cd apps/discovery-api && npx tsx --test src/lib/scouting.spec.ts` |
| 5 | `53250eb` | judge deferral w/ exponential backoff (`deferredJudge` map + `pending-review?excludeIds=`), `/internal/question-items/quarantine` (revokes applicability, never rewrites content), sampling honours `validThrough` | `npm run test:quality` (green) + typecheck |
| 6 | `84f0064` | `edge/edge-watchdog`: Workers Free cron `*/5`, KV state, metadata-only serial probes, state-change-only webhook alerts, local type shims, `tsc --noEmit` clean | `cd edge/edge-watchdog && npx tsc --noEmit -p tsconfig.json` |
| 7 | `d3d1d3b` | `cf-aig-collect-log-payload: false` on Gemini+OpenAI calls; `docs/quizzeira-pipeline-runbook.md` | grep for header string |
| 8 | `2d2602a` | prod readiness gate (§9): `checkProviderReadiness()` in worker-kit (`readiness.ts` + `configuredProviderNames()`); startup readiness log in content-worker (generation pass only), content-quality, quiz-corrector; `providerReadiness` surfaced on content-api `/admin/health` (same Redis circuit state) | worker-kit/content-worker/content-quality/quiz-corrector `tsc --noEmit` clean; `npm run test:quality` 107/107; content-api test 3/3 |
| 9 | `299bed5` | durable job lease layer, content-api half (§5, item 1a): generic `Job` table + additive migration `20260919010000_job_lease` (kind/dedupeKey/status queued\|leased\|deferred\|done\|dead/priority/availableAt/leaseOwner+leaseExpiresAt/attempts+maxAttempts/lastErrorCode+lastError/correlation+causation); `src/lib/jobs.ts` (`enqueueJob` idempotent-by-dedupeKey, `claimJobs` via `FOR UPDATE SKIP LOCKED` reclaiming expired leases, `completeJob`/`deferJob`/`failJob` with `computeBackoffMs` exponential-full-jitter → `dead` on `maxAttempts`); `/internal/jobs/{enqueue,claim,:id/complete,:id/defer,:id/fail,dead}` routes (workers still only ever reach content-api over HTTP, per §3.2). Content-worker passes are **not yet** converted to claim from this table — still direct entity polling. | `cd apps/content-api && node --import tsx --test src/lib/jobs.spec.ts` (6/6); `npm run test:content` 157/161 (4 pre-existing failures, see below); schema validated + migration DDL diffed byte-for-byte against `prisma migrate diff --from-empty` (see gotcha below re: generated-client typecheck) |
| 10 | `d97b110` | durable job lease layer, discovery-api half (§5, item 1a done): identical `Job` model/migration/`jobs.ts`/routes in discovery-api's own `quizzeira_discovery` database — no table sharing across planes. discovery-crawler still polls entity status directly; only item 1b (content-worker claim wiring) remains open under item 1. | `cd apps/discovery-api && node --import tsx --test "src/**/*.spec.ts"` (11/11: 6 new job-lease + 5 pre-existing scouting); migration DDL diffed byte-for-byte against `prisma migrate diff --from-empty` |
| 11 | `6565752` | first real job-claim consumer (§5, item 1b, partial): discovery-api's `POST /internal/artifacts` now enqueues one `import_artifact` Job per artifact at creation (dedupeKey'd); new idempotent `POST /internal/artifacts/backfill-import-jobs` for pre-migration artifacts (**not yet invoked against the live DB**). content-worker's `runImportPass` claims via `/internal/jobs/claim` instead of polling `?published=false`, then `complete`/`fail`s explicitly; `published` still set on success for other readers. `import.spec.ts` guards the `JOB_KIND`/`IMPORT_ARTIFACT_JOB_KIND` string contract between the two files so they can't drift silently. Only this one pass converted — `runProcessPass`/`runPlannerPass`/embeddings/generation and all of discovery-crawler's passes are still direct polling. | `node --import tsx --test apps/content-worker/src/stages/import.spec.ts` (4/4); full `test:content` 161/165 (4 pre-existing baseline failures, unchanged); content-worker `tsc --noEmit` clean; discovery-api `enqueueJob` call verified against a freshly generated client (see workaround below) |
| 12 | `be34714` | canonical-bank backfill wiring + dual-read parity check (§7, item 3 done): `src/lib/parity.ts` (`findParityGaps`, pure set-difference, unit tested); `checkCanonicalDualReadParity(examSlug)` in `canonical.ts` compares legacy-reachable published questions vs. ones reachable through active `QuestionApplicability`; `GET /internal/canonical/parity-check?examSlug=`; `scripts/backfill-canonical-bank.sh` (backfill → status → per-exam parity check, non-zero exit + WARNING on any gap) — run this against the live content-api (infra's job, this repo only ships the script) after any deploy touching the canonical bank, before dropping `/published/sample`'s legacy query path for an exam. | `node --import tsx --test apps/content-api/src/lib/parity.spec.ts` (4/4); full `test:content` 165/169 (same 4 pre-existing baseline failures); script verified end-to-end against a local mock server for clean/gap/no-args cases; `canonical.ts`/`parity.ts` verified against a freshly generated client |
| 13 | `9e42811` | poison-leaf guard in generation planner-queue (§9, item 2 partial): discovered the planner-queue **already** round-robins across syllabi/exams by registration deadline — that half of weighted fair scheduling pre-dates this rollout. Added the missing half: `src/lib/generation-fairness.ts` (`isLeafPoisoned`, pure, unit tested) skips a leaf whose last 3 `GenerationRun`s all failed (deficit never closes on its own, so it would otherwise claim a slot every pass forever); any ok/partial resets the streak. One extra query per syllabus in `GET /internal/generation/planner-queue` over leaves already fetched. **Not done**: per-minute/day generation+judge rate ceilings, queue depth/age into `StageMetric` — larger, separate pieces. | `node --import tsx --test apps/content-api/src/lib/generation-fairness.spec.ts` (5/5); full `test:content` 170/174 (same 4 pre-existing baseline failures); `generation.ts` query verified against a freshly generated client |
| 14 | `fcad670` | per-stage per-minute rate ceilings for generation/judge (§9, item 2 further along): the existing `LLM_RATE_LIMIT_PER_MINUTE` (slice 1) is global across every stage, so it didn't actually give generation/judge their own literal "one per minute" ceiling from the spec — a burst of cheap `ku`/`classify` calls could crowd them out without ever tripping the shared limit. `consumeStageRateLimit(stage)` in worker-kit's `llm.ts` adds `LLM_RATE_GENERATION_PER_MINUTE`/`LLM_RATE_JUDGE_PER_MINUTE` (default 1 each), same non-incrementing Lua-reservation pattern as the existing per-stage daily token budget, called once per `generateJson()` call (not per model-failover attempt), skipped on a cache hit like the global budget already is. **Still not done under item 2**: queue depth/age into `StageMetric`. | worker-kit `tsc --noEmit` clean; `npx vitest run` 72/73 (same pre-existing Redis-dependent fixture failure); 2 new cases (generation ceiling independent of global limit; judge ceiling independent of generation's); content-worker/content-quality `tsc --noEmit` clean; `npm run test:quality` 107/107 |
| 15 | `4a70549` | edge-watchdog test coverage (§14, item 8 partial): the Worker shipped in slice 6 had **zero** automated tests — the "one state-change alert" acceptance criterion was only ever eyeballed. New `src/index.spec.ts`: in-memory `KVNamespace` fake + scripted `fetch` stub drive the real `scheduled()` handler through synthetic failure/recovery sequences, no live Cloudflare account needed. All 5 cases passed against the existing implementation unmodified (regression coverage, not a bugfix). Split `tsconfig.json` (deploy-facing, WebWorker lib, now excludes `*.spec.ts`) from new `tsconfig.spec.json` (Node-typed, for the test file only); new local `package.json` (still outside the npm workspaces glob — doesn't touch root install/typecheck). | `cd edge/edge-watchdog && npm test` (5/5) and `npm run typecheck` (both tsconfigs clean) |
| 16 | `b826287` | StageMetric service tagging (§12 item 4 unblocks): found item 4 ("compare per-stage StageMetric of split vs monolith") was silently impossible — `StageMetric` had no dimension distinguishing which process wrote a row, so the compose `shadow` profile's separate `content-worker-documents/-embeddings/-generation` instances and the monolith all aggregated into the same counters (Loki logs already had `SERVICE_NAME`; this Postgres table didn't). Additive migration `20260919120000_stage_metric_service` adds `service` (default `''`) and widens the unique constraint to include it — safe against existing data, verified with `prisma migrate diff --from-schema-datamodel` (old vs new schema, not `--from-empty` this time since the table already has rows in concept). `recordStageMetric` takes `service`; content-worker's `emitStageMetric` sends its own `SERVICE_NAME`; `GET /internal/stage-metrics` gained `?service=` filtering. **Not done**: actually running the comparison (needs real traffic through a live `shadow` profile) and flipping the default profile — operator/infra action. | `node --import tsx --test apps/content-api/src/lib/stage-metrics.spec.ts` (3/3, duck-typed fake client, no DB); full `test:content` 173/177 (same 4 pre-existing baseline failures); content-worker `tsc --noEmit` clean; migration DDL re-verified byte-for-byte |
| 17 | `fe81a73` | runbook alert map update (§10.3, item 7 partial): the "dead-letter view per plane" row predated the generic `Job` table (written in slice 7, before slices 9-10) — pointed it at the real `GET /internal/jobs/dead` now. Added two rows an operator would otherwise misdiagnose as incidents: a leaf stuck despite a positive deficit (poisoned per slice 13 — check `GenerationRun.error`, don't just retry) and generation/judge throughput capped near 1/min (slice 14's rate ceiling, by design). Added the `?service=` comparison recipe (slice 16) to Diagnostic entry points. Fixed a typo. **Not done under item 7**: actual Grafana dashboards (infra-repo territory per CLAUDE.md's Live/infra split), AI Gateway account-level config (a Cloudflare account action, not a header — the header was already slice 7), workers-AI golden-set benchmark (opt-in, no one has opted in). | doc-only; read the diff |

Repo note: worktree commits were cherry-picked into `/home/ferre/Code/quizzeira` (origin).
Worktree at `/home/ferre/Code/.codex-worktrees/quizzeira-autonomy` contains the originals (`codex/quizzeira-autonomy`) — future work should be committed on the PR branch in the main checkout to avoid re-sync.
Conclusion: work on the branch in `/home/ferre/Code/quizzeira` from now on; push to PR branch there so the draft PR accumulates commits correctly.

## Retry-against-baseline test instructions

`apps/content-worker` tests hang unless you disable the host REDIS_URL; always run with:
```bash
cd apps/content-worker && REDIS_URL= LLM_PROVIDER=fixture npx tsx --test "src/**/*.spec.ts"
```
Pre-existing failures (identical on baseline, pre-change) — do not investigate as regressions:
- 4 content-worker golden/role-classification failures (`EDT-002`, `KNW-002`, fuzz §41.6, edital-estadual-tce leaf coverage)
- 1 worker-kit `fixture LLM provider > yields a draftable MCQ` (unreachable `redis://redis:6379` from `.env` in test env)

**Environment gotcha (this host, 2026-09-19):** `packages/shared/dist/`, `apps/discovery-api/src/generated/prisma/`, and `apps/content-api/src/generated/prisma/` had root-owned files (leftover from a `docker compose` build run as root), blocking `npm run typecheck` / `npm run db:generate` with `EACCES`. Removing `packages/shared/dist` worked (plain user owned the dir); the two `generated/prisma` dirs are root-owned themselves and need `sudo chown -R $USER` before `db:generate` will work — user was asked again on 2026-09-19 (slice 9) and again declined, so it's still unfixed. Until fixed, verify changed packages in isolation: `cd <pkg> && npx tsc --noEmit -p tsconfig.json`. The 173 content-api and ~60 discovery-api repo-wide typecheck errors you'll see are this stale-client issue, not real regressions — confirmed via `git stash` against baseline.

**Workaround that does work without touching the root-owned dir (used for slice 9):** point a copy of `prisma/schema.prisma` at a sibling output dir outside `src/generated/` (e.g. `output = "../prisma-check-client"`, schema file kept inside `prisma/` so project-root inference still works), `prisma generate --schema prisma/schema.check.prisma`, then `tsc --noEmit` a scratch copy of the new file against that fresh client. Delete both scratch artifacts afterward — never commit them. Also usable standalone: `prisma validate` / `prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` need real-looking (but fake) `*_DATABASE_URL`/`*_DATABASE_DIRECT_URL` env values inline — they don't hit a real DB, just parse the schema — and `migrate diff` is the fastest way to confirm a hand-written migration's DDL matches what Prisma would generate.

## Next (pick topmost unfinished)

**Status as of slice 17 (2026-09-19):** everything safely doable from a sandbox with no live DB/Redis/Cloudflare/provider access has been done. What's left under items 1b/4/5/6/7/8/9 is now overwhelmingly *operator or infra action*, not code: running `docker compose --profile shadow up` with real traffic (4), a full admin-review cycle against real `SourceCandidate` data (5), a live canary (6), provisioning Grafana dashboards in the infra repo (7), an actual Cloudflare KV+secrets+deploy (8), and PR graduation once the canary passes (9). An agent picking this up should re-scan for anything newly codeable before assuming that's still true, but don't manufacture busywork (e.g. mechanically converting a pass that already works, or adding metrics no one will read) just to keep the loop moving — flag the operator-blocked items to a human instead.

1. [ ] **durable job lease layer — remaining sub-steps** (tables+APIs: slices 9-10; first consumer: slice 11):
   - [x] 1a. discovery-api job table — done, slice 10.
   - [ ] **1b. worker claim wiring — in progress, re-scoped 2026-09-19.** Done: content-worker's `runImportPass` (slice 11) — it fit the generic Job model well because its old "queue" was a bare `published` boolean with **no** reclaim-on-crash mechanism at all, which is exactly the gap Job closes.
     - **Finding (read before converting `runProcessPass` next):** `runProcessPass`'s queue is `Document.status` (`pending`/`extracting`/`extracted`/`failed`), and it already has its own bespoke lease: `GET /internal/extraction/queue` reclaims rows stuck in `extracting` past `STALE_EXTRACTING_MS` (15 min, see `apps/content-api/src/routes/internal/documents.ts`), plus `attempts`/`failReason`/transient-vs-final retry logic baked into the pass itself (including a budget-exhaustion `break` that stops the whole batch, not just one row — no equivalent in the generic model). Converting it to generic `Job` claims would mean re-deriving all of that against a table with no direct test coverage today (`process.ts` itself has zero spec file; coverage is indirect through golden/invariant tests), for a queue that isn't actually missing a recovery mechanism. **Do not convert `runProcessPass`/`runPlannerPass`/embedding/generation passes mechanically just to "finish 1b"** — each shares this same already-working polling+reclaim pattern. Only convert one if a concrete problem with its current mechanism shows up (e.g. a real starvation/duplicate-processing incident), and if so design around its specific retry semantics rather than reusing import's shape as-is.
     - [x] **checked discovery-crawler, 2026-09-19: do not convert.** Its per-pass Source selection (`selectSourcesForPass` in `apps/discovery-crawler/src/source-order.ts`) already rotates by least-recently-succeeded (topic-query sources always ride along), and each source reports `/internal/sources/:id/health` with its own `failCount`/`status` tracking — a materially different, already-reasonably-fair mechanism, not the "no recovery at all" gap `import.ts` had. Nothing to fix here without a concrete incident.
     - [ ] **run `POST /internal/artifacts/backfill-import-jobs` once against the live DB** before/when deploying slice 11, so artifacts created before this migration still get imported (new artifacts already self-enqueue; this only back-fills the pre-existing `published=false` backlog).
   - [ ] **1c. dead-letter visibility**: `GET /internal/jobs/dead` exists (both planes) but nothing surfaces it yet — wire into the §10.3 dashboard work (item 7) rather than building a one-off view now.
2. [ ] **weighted fair scheduling** (§9) — partial, slice 13:
   - [x] exam fairness — was already implemented (planner-queue round-robins syllabi by registration deadline; pre-dates this rollout).
   - [x] poison-leaf guard — done, slice 13 (`isLeafPoisoned`).
   - [x] per-minute rate ceilings for generation batches and judge items — done, slice 14 (`LLM_RATE_GENERATION_PER_MINUTE`/`LLM_RATE_JUDGE_PER_MINUTE`, default 1 each). Daily ceilings were already covered by the pre-existing per-stage `LLM_BUDGET_*_TOKENS` (token, not call, count — close enough to "daily ceiling below provider quotas" that a separate daily call-count cap wasn't added).
   - [ ] queue depth/age into `StageMetric` — not implemented.
3. [x] **backfill wiring** — done, slice 12: `scripts/backfill-canonical-bank.sh` + `GET /internal/canonical/parity-check`. Still needs an actual run against the live DB (script ships in this repo; running it is infra/operator's job) before any exam's legacy sampling path gets dropped.
4. [ ] **shadow metrics report** — unblocked, slice 16: `StageMetric` now carries `service`, so `GET /internal/stage-metrics?service=quizzeira-contentworker` vs `?service=quizzeira-contentworker-documents` (etc.) over the same window is the actual comparison. Still needs: `docker compose --profile shadow up`, real traffic through both, then flip the default `CONTENT_WORKER_PROFILE` (§12 slice 2 exit) — operator/infra action, not something to do from a sandbox without a live compose stack.
5. [ ] **scouting autonomous enablement** (§12 slice 4 exit): compare observe-only decisions vs admin review for a full cycle → then set `DISCOVERY_SCOUT_AUTO_ACTIVATE=true` in infra prod only with the wave-2 backoff bands (1d/3d/7d/30d probes; source suspend; blocklist expiry).
6. [ ] **canary (§12 verification)**: one topic, one exam, concurrency 1; acceptance per spec §14 (24 h stable memory, no overlap, correct backoff, full log correlation, bounded provider calls, one published/rejected outcome).
7. [ ] **dashboard + alerts wiring (§10.3)** — partial, slice 17:
   - [x] alerts listed in runbook table — updated, slice 17 (`docs/quizzeira-pipeline-runbook.md`).
   - [ ] Grafana overview + per-correlation drill-down — infra-repo territory (dashboards aren't provisioned from this repo; this repo only emits the metrics/logs Grafana would read).
   - [ ] AI Gateway account-level config (payload logging disabled) — the per-request header (`cf-aig-collect-log-payload: false`) shipped in slice 7; the account-level toggle is a Cloudflare account action.
   - [ ] workers-AI golden-set benchmark — only if someone opts in to that adapter; no one has.
8. [ ] **Edge watchdog deploy** — partial, slice 15:
   - [x] synthetic public-health failure fixture for the "one state-change alert" acceptance criterion — done (`edge/edge-watchdog/src/index.spec.ts`).
   - [ ] take `wrangler.toml` out of `replace-me` KV id, set secrets (`WATCH_TARGETS`/`ALERT_WEBHOOK`/`WATCH_SECRET`) — this is a real Cloudflare account action (create a KV namespace, run `wrangler secret put`, `wrangler deploy`); do not do this without the operator explicitly asking, even though Cloudflare MCP tools are available in this session.
9. [ ] **PR graduation**: when canary exits clean, drop draft → ready for review; then land and deploy per infra repo (not this repo).

## Working conventions

- Query first, never edit blind; smallest diff.
- Commit message format: `feat(scope): what (§12 slice N)` like prior commits.
- Push from `/home/ferre/Code/quizzeira` on branch `feat/autonomous-discovery-question-quality`; PR title/body already set.
- Typecheck after each slice: `npm run typecheck`. Full Nx tests only when change spans planes.
- **UPDATE `next` marker after every completed step** — append the next goal line.

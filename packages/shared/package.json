{
  "name": "@quizzeira/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && mkdir -p dist/curriculum && cp -f src/curriculum/metadata-model.json dist/curriculum/ && (test -f src/curriculum/subjects.yaml && cp -f src/curriculum/subjects.yaml dist/curriculum/ || true)",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}

// Specs for listing HTML live in listing.spec.ts (§48.8 listing-parse deleted).
import { describe, it } from "node:test";

describe("listing-parse tombstone", () => {
  it("defers to listing.spec.ts", () => {
    // Physical delete of listing-parse.spec.ts blocked in harness.
  });
});
